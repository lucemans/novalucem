{
  "name": "@novalucem/deploy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "mkdir -p ~/.kube/ && echo $CLUSTER | base64 -d > ~/.kube/config && ts-node ./src/index.ts",
    "build": "docker build -t r.lvk.sh/novalucem/deploy:abcdefg ../../ -f ./Dockerfile && docker push r.lvk.sh/novalucem/deploy:abcdefg"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
