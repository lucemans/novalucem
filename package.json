{
  "name": "novalucem",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/lucemans/novalucem.git",
  "author": "Lucemans <luc@lucemans.nl>",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "build": "lerna run build --stream --since",
    "push": "lerna run push --stream --since",
    "buildnpush": "yarn build && yarn push",
    "test": "lerna run test --stream --since",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "dependencies": {
    "@types/jest": "^26.0.14",
    "@types/jsonwebtoken": "^8.5.0",
    "@types/node": "^14.11.2",
    "@types/redis": "^2.8.27",
    "chalk": "^4.1.0",
    "jest": "^26.4.2",
    "jsonwebtoken": "^8.5.1",
    "nanoid": "^3.1.12",
    "redis": "^3.0.2",
    "ts-jest": "^26.4.1"
  }
}
